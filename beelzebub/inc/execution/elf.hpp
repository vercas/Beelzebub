#pragma once

#include <metaprogramming.h>
#include <handles.h>

namespace Beelzebub { namespace Execution {
    /**
     *  Represents the header of a 32-bit ELF file.
     */
    struct ElfHeader
    {

    };
}}
